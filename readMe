Firstly data set is red into repository. Training and test data sets are combined row-wise. Measurements related to mean and standard deviation are extracted through searching variable names for relevant key words. Activity names are attached by looking up table, which searches the number in the label file. Subjects’ number and activity labels are attached in front column-wise. Variable names are assigned firstly using features file and names () function for data frame. The names are then made more descriptive by string replacing. However, acc,gyro,std are stilled used as abbreviation for shorter names.